// Custom cursor — pure CSS, zero JS animation overhead.
// The previous Framer Motion spring-animated version caused
// layout thrashing on every mousemove, which blanked the page on scroll.
export default function CursorFollower() {
  return null;
}
